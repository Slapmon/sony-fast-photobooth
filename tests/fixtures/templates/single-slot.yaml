# Test-only fixture: single-slot template used by test_session_manager.py to
# prove shot_count == 1 behaves exactly like the pre-T-2.6 single-shot flow.
name: "Test Single Slot"
canvas:
  width_mm: 152
  height_mm: 102
  dpi: 300
  background: "#ffffff"
slots:
  - { x: 0, y: 0, w: 1795, h: 1205, fit: cover }
overlays: []
variants:
  print: { dpi: 300, format: jpeg, quality: 95 }
  web:   { long_edge: 2000, format: jpeg, quality: 85 }
  thumb: { long_edge: 400,  format: jpeg, quality: 75 }
